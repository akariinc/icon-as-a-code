
//- ラジオボタン

mixin c-radiobutton(args = { id: "", name: "", label: "", value: "", selected: false })
  - var base = "c-radio";
  div(class=base) 
    - if (args.selected)
      input(type="radio", id=args.id, name=args.name, value=args.value checked)
    - else
      input(type="radio", id=args.id, name=args.name, value=args.value)
    label(for=args.id) !{args.label}